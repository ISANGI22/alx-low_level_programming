#include "main.h"
#include <string.h>
#include <publib.h>

/**
 * rev_string - function that reverses a string
 * @s: parameter to be used
 * Return: Always 0
 */
void rev_string(char *s)
{
	strrev(s);
}
